{
  "buildCommand": "npx prisma generate && (npx prisma db push --accept-data-loss || echo 'DB push skipped') && (npx tsx prisma/seed.ts || true) && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
}
